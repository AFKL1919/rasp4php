{
    "APP_CONFIG":{
        /* If it is empty, a random value will be generated. */
        "SECRET_KEY": "",
        /* Unit minutes */
        "PERMANENT_SESSION_LIFETIME": 5
    },
    "DB":{
        "SQLALCHEMY_DATABASE_URI": "sqlite:////tmp/test.db"
    }
}